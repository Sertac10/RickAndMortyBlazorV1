@page "/Locations"
@inject RickAndMortyBlazorV1.Service.IRickAndMortyService rickMortyApi
@inject IJSRuntime js

@* 
@foreach (Location location in LocationList)
{
    

} *@
@* <img src="@ImageUri" /> *@

@code{

    public List<Location> LocationList { get; set; } = new();
    public string ImageUri { get; set; } = string.Empty;
    private string searchTerm = "Earth (C-137)";
    private string randomImageUrl;
    protected override async Task OnInitializedAsync()
    {
        // var locations =  await rickMortyApi.GetAllLocations();
        // locations.ToList();

        // await GetRandomImage.GetRandomGoogleImage("rick");
        // foreach(Location location in locations)
        // {
        //     ImageUri = await GetRandomImage.GetRandomGoogleImage(location.Name);
        //      await Task.Delay(1000);
        //      StateHasChanged();
        // }
        // await SearchRandomImage(searchTerm);
        
    }

    // private async Task SearchRandomImage(string searchTerm)
    // {
    //     // JS tarafındaki getRandomGoogleImage fonksiyonunu çağırıyoruz
    //     randomImageUrl = await js.InvokeAsync<string>("getRandomBingImage", searchTerm);
    // }


    }


